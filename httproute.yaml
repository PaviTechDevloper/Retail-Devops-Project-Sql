apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: capsule-route
  namespace: pavi18-ns
spec:
  parentRefs:
  - name: capsule-gateway
  rules:
  - matches:
    - path:
        type: PathPrefix
        value: /retail
    backendRefs:
    - name: capsule-service
      port: 80