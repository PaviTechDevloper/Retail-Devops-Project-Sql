apiVersion: apps/v1
kind: Deployment
metadata:
  name: mysql
  namespace: pavi18-ns
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mysql
  template:
    metadata:
      labels:
        app: mysql
    spec:
      containers:
      - name: mysql
        image: mysql:8.0
        ports:
        - containerPort: 3306
        env:
        - name: MYSQL_ROOT_PASSWORD
          value: "rootpassword"
        - name: MYSQL_DATABASE
          value: "capsule_db"
        - name: MYSQL_USER
          value: "capsule_user"
        - name: MYSQL_PASSWORD
          value: "capsule_password"
        volumeMounts:
          - mountPath: /app/data
            name: retail-storage

      volumes:
        - name: retail-storage
          persistentVolumeClaim:
            claimName: retail-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: mysql-service
  namespace: pavi18-ns
spec:
  selector:
    app: mysql
  ports:
  - port: 3306
    targetPort: 3306
  type: ClusterIP
